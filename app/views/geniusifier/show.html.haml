- if @rendered.present?
  %button#copy-button{'data-clipboard-text' => h(@rendered), title: "Click to copy"} Copy!
  .geniusified
    =preserve @rendered

%h2 Paste in some markdown

= form_for :geniusifier, :url => geniusifier_url do |f|
  = text_area_tag :markdown, @markdown, rows: 20, cols: 100
  %br
  = submit_tag 'Submit'
